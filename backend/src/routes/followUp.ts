/**
 * DEPRECATED — The /api/follow-up-questions route has been removed from the
 * spec. This file is kept for git history but is no longer mounted in server.ts.
 *
 * All imports and exports are intentionally removed to prevent type errors
 * from the now-deleted FollowUpRequest / FollowUpResponse types.
 */
